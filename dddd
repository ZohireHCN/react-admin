CdcOptimized templateCdc = templateCdcRef.get();

if (templateCdc == null) {
    CdcOptimized newTemplateCdc = buildTemplateCdc(...);

    if (templateCdcRef.compareAndSet(null, newTemplateCdc)) {
        templateCdc = newTemplateCdc;
    } else {
        templateCdc = templateCdcRef.get();
    }
}